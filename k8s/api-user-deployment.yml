apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-user
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-user
  template:
    metadata:
      labels:
        app: api-user
    spec:
      containers:
        - name: api-user
          image: pazetox/udacity-microservices:api-user
          ports:
            - containerPort: 8080
          env:
            - name: POSTGRES_USERNAME
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "99277032"
            - name: POSTGRES_HOST
              value: "database-udacity-lab.clrjhrrjum3x.us-east-1.rds.amazonaws.com"
            - name: POSTGRES_DB
              value: "postgres"
            - name: JWT_SECRET
              value: "testing"
